# Deprecated

`fantasy-pixel-art`, `spell-fx`, and every `speckit-*` skill here moved to
[`kamjan60/claude-toolkit`](https://github.com/kamjan60/claude-toolkit), the
shared toolkit repo pulled into `dread-deck` and `i-am-not-a-wizard-harry`.
Edit them there — this copy is unmaintained (kept as-is only so this repo's
own Claude Code sessions keep working).
